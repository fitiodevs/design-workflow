---
name: template-skill
description: Replace with a description of the skill and when Claude should use it. Include EN + PT triggers and natural-language phrases that should fire it. Spec-compliant frontmatter has only `name` and `description` — do NOT add `license:` or `triggers:` (both are non-standard and silently ignored by the harness).
---

# Skill: <name> (`/<command>`) — persona **<PT>** (English: **<EN>**)

## Triggers

- **English:** `/<EnglishPersona>`, `/<command>`, "natural english phrase", "another phrase"
- **Português:** `/<PersonaPT>`, `/<persona-pt>`, `/<command>`, "frase natural pt-BR", "outra frase"
- **Natural language:** describe the kind of free-form requests this skill should catch

# Insert instructions below
