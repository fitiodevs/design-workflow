# Flutter theme-extend smoke — v1.2.0 (T-22)

> Byte-equivalence regression for `/theme-extend` on Flutter stack.

## Setup

- Stack: `flutter` (default)
- Operation: add a new token `feedbackPositive` (light + dark hex pair) via `/theme-extend`
- Baseline: pre-v1.2 `app_colors.dart` snapshot from the Fitio repo

## Harness

```bash
# 1. Resolve stack (expect: flutter)
STACK=$(python3 scripts/resolve_stack.py) && [ "$STACK" = "flutter" ]

# 2. /theme-extend emits Plan
#    Expected sidecar: /tmp/extend-feedbackPositive-plan.json (kind: palette,
#    actions: patch palette + append design-tokens)

# 3. Adapter renders to lib/core/theme/app_colors.dart
python3 adapters/flutter/adapter.py /tmp/extend-feedbackPositive-plan.json

# 4. Byte-diff (modulo whitespace)
diff -u --ignore-trailing-space \
    <(git show pre-v1.2.0:lib/core/theme/app_colors.dart) \
    lib/core/theme/app_colors.dart
# Expect: only the new feedbackPositive lines + the corresponding hex pair.
```

## Acceptance

- [ ] `/theme-extend` produces a Plan JSON sidecar (Adapter Plan v1.0 schema).
- [ ] Adapter writes `lib/core/theme/app_colors.dart` with the new token in the field/ctor/_light/_dark blocks.
- [ ] `flutter analyze` returns `No issues found!`.
- [ ] Diff against pre-v1.2 baseline shows ONLY the additions for `feedbackPositive` (no churn elsewhere).
- [ ] `docs/design-tokens.md` has the new row.
