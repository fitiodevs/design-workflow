# Flutter port smoke — v1.2.0 (T-18)

> Adapter dispatch regression smoke for Fitio dogfood path.

## Setup

- Stack: `flutter` (default; no `STACK` env, no `stack:` override)
- Plan kind: `widget-tree` from a Figma frame already ported in v1.1.x
- Reference snapshot: `.design-spec/regression-snapshots/pre-v1.2/flutter/`

## Harness

```bash
# 1. Resolve stack (expect: flutter)
STACK=$(python3 scripts/resolve_stack.py) && [ "$STACK" = "flutter" ]

# 2. Emit Plan from theme-port (manual: open Claude Code in Fitio project,
#    invoke `/theme-port <figma-url>`, then verify the Plan JSON sidecar)

# 3. Adapter renders to lib/ paths
python3 adapters/flutter/adapter.py /tmp/coupons-plan.json

# 4. Byte-diff
diff -r --strip-trailing-cr .design-spec/regression-snapshots/pre-v1.2/flutter/ \
                            lib/features/coupons/presentation/widgets/
```

## Acceptance

- [ ] Step 2 produces a Plan JSON conforming to `docs/adapter-plan.schema.json`
- [ ] Step 3 prints the expected widget path
- [ ] Step 4 returns zero diff (modulo trailing whitespace)

## Notes

This file is the **harness contract**. The actual diff output is captured during the Fitio-side dogfood run; results land in the v1.2.0 release notes. The contract itself was verified by running `adapters/flutter/tests/conformance.py` (3/3 PASS) against the canonical Plan examples in `docs/adapter-examples/`.
