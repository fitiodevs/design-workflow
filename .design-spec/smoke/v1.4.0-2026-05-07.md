# v1.4.0 smoke log

**Date:** 2026-05-07
**Branch:** correcoes
**Pre-rollback tag:** `pre-v1.4.0`
**Upstream SHA at fork time:** `9c64ef1b2bb2cffafb0ad6167d6a3831bdc0ba11`

## T-20 — `/tweaks` automated smoke

The interactive browser portion is reserved for the user (Chrome + Firefox manual run). The structural smoke below verifies that the wrapping logic produces a well-formed self-contained HTML.

**Fixture:** `/tmp/milestone-smoke.html` — 35 lines, tweaks-ready (CSS custom properties drive everything, `data-od-id="hero"` on the section, light/dark via `:root[data-mode]`).

| Check | Expected | Actual | Status |
|---|---|---|---|
| Sibling output written | `/tmp/milestone-smoke.tweaks.html` | created (10,518 bytes) | ✅ |
| Original input untouched | `/tmp/milestone-smoke.html` byte-equivalent | 1,427 bytes (unchanged) | ✅ |
| `%BASENAME%` placeholder substituted | 0 remaining | 0 | ✅ |
| `%CONTENT_SHA%` placeholder substituted | 0 remaining (replaced with `acdcb676`) | 0 | ✅ |
| Panel root `id="dw-tweaks-panel"` injected | present | present | ✅ |
| 5 fieldsets (one per knob) | 5 | 5 | ✅ |
| All 5 knob ids present (`dw-knob-{hue,scale,density,mode,motion}`) | yes | yes | ✅ |
| Injected `<script>` parses (Node `new Function()`) | OK | OK | ✅ |
| File size below 100KB target | <100KB | 10.5KB | ✅ |

## T-20 — manual user steps (interactive, run by user)

The model cannot drive a browser. Once committed, the user runs:

1. `xdg-open /tmp/milestone-smoke.tweaks.html` (or `firefox /tmp/milestone-smoke.tweaks.html`).
2. Confirm panel appears top-right.
3. Drag accent hue slider — header `<h1>` color shifts live.
4. Click "1.333 (open)" radio — `<h1>` font-size jumps up.
5. Switch theme to dark — body bg goes near-black.
6. Reload tab — state persists (panel restores the slider/radio positions).
7. Click Reset — defaults restored (hue=18, scale=1.250, density=comfortable, light, motion on).
8. Repeat in Firefox — same outcome.
9. Open DevTools → localStorage → confirm key `tweaks:milestone-smoke:acdcb676` exists with the JSON state.

If any of steps 3–8 fail, file an issue against `skills/tweaks/assets/panel.html`.

## T-21 — `/theme-critique --mode 5dim` automated smoke

| Check | Expected | Actual | Status |
|---|---|---|---|
| Output path | `.design-spec/critique/milestone-smoke/<ts>-5dim.html` | `2026-05-07-1255-5dim.html` written | ✅ |
| Self-contained HTML (no external assets) | yes | yes (inline `<style>` + inline `<svg>`) | ✅ |
| Inline radar chart (`<svg`) | present | present | ✅ |
| 5 dimension cards | 5 | 5 | ✅ |
| Keep / Fix / Quick wins lists | all 3 sections | all 3 | ✅ |

The smoke report uses synthetic scores (Philosophy 7 / Hierarchy 6 / Detail 7 / Function 8 / Innovation 6) over the fixture mockup. A real invocation by the model on a Clara mockup would derive scores from evidence cited inline.

## Combined verdict

**Tweaks:** structural smoke ✅. Interactive verification reserved for user.
**5dim critique:** structural smoke ✅. Output structure matches REQ-03.2.

No blocking issues. Proceeding to T-22 (commit + push + tag + release).
